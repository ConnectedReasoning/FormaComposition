{
  "_NOTE": "PREVIEW ONLY -- not yet renderable via main.py, same caveat as piece_apex_cadence.json. This JSON reconstructs the exact parameters used to generate sparse_with_apex.mid via a direct generate_melody_for_progression() call, for reference. Phase 6 (SectionModel schema field + generator.py passthrough) hasn't happened yet, so main.py doesn't know how to read melodic_arc out of a piece file yet -- this is the biased half of the Phase 5 A/B comparison, identical to piece_sparse_baseline.json except for the melodic_arc block below.",

  "piece": {
    "title": "Sparse With Apex Bias",
    "key": "C",
    "mode": "aeolian",
    "tempo": { "min": 70, "max": 70 },
    "seed": 55,
    "form_type": "song",
    "form": ["drift"],
    "sections": {
      "drift": {
        "bars": 24,
        "progression": ["i", "VI", "III", "VII"],
        "chord_bars": [2.0, 2.0, 2.0, 2.0],
        "_chord_bars_note": "cell totals 8 bars, tiles 3x to fill 24 bars -- matches the 3-cycle progression used to render sparse_with_apex.mid.",
        "density": "sparse",
        "melody": "sparse",
        "bass_style": "pedal",
        "arc": "plateau",
        "rhythm": "free",
        "note_length_range": { "min": 1.0, "max": 2.0 },
        "harmony_rhythm": { "rhythm": "sustain" },
        "rest_probability": 0.0,

        "melodic_arc": {
          "apex_degree": 4,
          "apex_position": 0.6,
          "resolve_every_cycle": true
        },
        "_melodic_arc_note": "resolve_every_cycle=true (Choice B): cadence pull fires at the end of every 4-chord progression cycle, all 3 of them, not just the section's true end -- chosen for this comparison specifically so the A/B contrast wasn't diluted by only one cadence point across 24 bars. apex_degree=4 (the dominant) with apex_position=0.6 gives the melody a declared peak roughly 60% through the section, same mechanism as generate_lyrical/generate_generative, applied here via generate_sparse's candidate-list weighting (see melody.py's Phase 5 docstring on generate_sparse for the explicit caveat: this is the one behavior where passing statistics don't settle whether the result still sounds like sparse -- that's a listening judgment, which is what this A/B pair is actually for)."
      }
    }
  }
}
